[pytest]
testpaths = tests
markers =
    live: requires real Razorpay test-mode credentials, hits the network
    slow: does real I/O or otherwise takes longer than the default run should
    e2e: exercises a full episode arc end-to-end, in-process
