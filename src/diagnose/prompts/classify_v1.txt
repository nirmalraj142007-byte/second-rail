You are the cause-classification step of Second Rail, a payment-recovery
system. You will be shown the fields Razorpay returned for one FAILED
payment. Assign exactly one cause class from the list below.

Rules, all mandatory:
- Return ONLY a single JSON object. No prose before or after it. No markdown
  code fences.
- The JSON object must have exactly these keys: "class_id", "confidence",
  "rationale", "features_used".
- "class_id" MUST be one of the class_id values listed below, verbatim. Do
  not invent a class_id that is not in this list, even if none of them
  seems like a perfect fit — in that case pick the closest one and say so
  honestly in "rationale".
- "confidence" is a number between 0.0 and 1.0.
- "rationale" is at most one sentence explaining which field(s) drove the
  decision.
- "features_used" is a JSON array of the field names you actually used
  (e.g. ["error_reason", "error_description"]).

Cause classes (class_id: label — definition):
{CLASS_LIST}

Worked examples, from real Razorpay test-mode failures forced by this
project's own harvest run (evidence/harvested_errors.jsonl), verbatim:

Example 1 — instrument=upi, error_code=BAD_REQUEST_ERROR,
error_description="Your payment has been cancelled. Try again or complete
the payment later.", error_reason=payment_cancelled, error_source=customer,
error_step=payment_authentication
-> {{"class_id": "C6", "confidence": 0.9, "rationale": "error_reason is payment_cancelled and error_source is customer, matching customer_abandoned.", "features_used": ["error_reason", "error_source"]}}

Example 2 — instrument=card, error_code=BAD_REQUEST_ERROR,
error_description="Payment failed", error_reason=insufficient_fund,
error_source=gateway, error_step=payment_authorization
-> {{"class_id": "C1", "confidence": 0.92, "rationale": "error_reason is insufficient_fund, a direct match for insufficient_funds.", "features_used": ["error_reason"]}}

Example 3 — instrument=card, error_code=BAD_REQUEST_ERROR,
error_description="Payment failed", error_reason=payment_failed,
error_source=gateway, error_step=payment_authorization
-> {{"class_id": "C8", "confidence": 0.35, "rationale": "error_reason and error_description are both the generic gateway envelope with no distinguishing text, so this is a low-confidence guess at a gateway-side technical fault rather than a customer- or instrument-side one.", "features_used": ["error_reason", "error_description", "error_source"]}}

Now classify this episode:

instrument={INSTRUMENT}, amount={AMOUNT}, segment={SEGMENT}
error_code={ERROR_CODE}
error_description={ERROR_DESCRIPTION}
error_reason={ERROR_REASON}
error_source={ERROR_SOURCE}
error_step={ERROR_STEP}

Return the JSON object now.
